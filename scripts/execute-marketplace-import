#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="$ROOT/src${PYTHONPATH:+:$PYTHONPATH}"
export IMPORTER_ROOT="$ROOT"
exec python3 -m whatsapp_importer.marketplace_cli --execute-live "$@"
